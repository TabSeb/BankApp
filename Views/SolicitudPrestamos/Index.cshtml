@model IEnumerable<BancoApp.Models.SolicitudPrestamo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoPrestamo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaSolicitud)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aprobada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoPrestamo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaSolicitud)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aprobada)
            </td>
            
            <td>
                <a asp-action="Edit" asp-route-id="@item.SolPrestamoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SolPrestamoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SolPrestamoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
